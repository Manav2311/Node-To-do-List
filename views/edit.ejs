<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Roboto", sans-serif;
        }

        @import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap');

        .main {
            width: 100%;
            min-height: 100vh;
            background: rgb(4,102,125);
background: radial-gradient(circle, rgba(4,102,125,1) 10%, rgba(103,208,233,1) 38%, rgba(45,6,144,1) 87%);
            padding: 10px;
        }

        .text-center {
            text-align: center;
        }

        .todo-list {
            width: 100%;
            max-width: 540px;
            background: darkgray;
            margin: 100px auto 20px;
            padding: 40px 30px 30px;
            border-radius: 10px;
        }

        .todo-list h2 {
            color: #002765;
            display: flex;
            align-items: center;
            margin-bottom: 20px;
            font-weight: bold;
        }

        .todo-list h2 img {
            width: 30px;
            margin-left: 10px;

        }

        a img {
            width: 30px;
            margin-left: 10px;
            margin: 5px 0 3px 15px;
        }

        .form {
            display: flex;
            align-items: center;
            background: #edeef0;
            border-radius: 30px;
            padding-left: 20px;
            margin-bottom: 15px;
        }

        input {
            flex: 1;
            border: none;
            outline: none;
            background: transparent;
            padding: 10px;
        }


        button {
            border: none;
            outline: none;
            padding: 12px 50px;
            background: #ff5945;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            border-radius: 40px;
/*            margin-bottom: 20px;*/
        }
    </style>
</head>

<body>
    <div class="main">
        <div class="todo-list">
            <h2>To-Do List<img src="/images/pencil.png"></h2>
            <form action="/insertData" method="post">
                <div class="form">
                  <input type="hidden" name="editId" value="<%= data.id %>">
                    <input type="text" name="task" placeholder="Add Your Task" value="<%= data.task %>">
                    <button type="submit">Update</button>
                </div>
            </form>
        </div>
    </div>
    </section>
</body>

</html>